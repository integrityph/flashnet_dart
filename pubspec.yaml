name: flashnet_dart
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  boringssl_ffi:
    git:
      url: https://github.com/integrityph/boringssl_ffi
      ref: v0.2.0
  ffi: ^2.1.4
  spark_dart:
    git:
      url: https://github.com/integrityph/spark_dart
      ref: v0.1.3
  secp256k1_ffi:
    git:
      url: https://github.com/integrityph/secp256k1_ffi
      ref: v0.1.1
  bip32_key_derivation:
    git:
      url: https://github.com/integrityph/bip32_key_derivation
      ref: v0.1.1
  bolt11_decoder: ^1.0.2
  http: ^1.6.0
  crypto: ^3.0.7
  bech32: ^0.2.2
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
